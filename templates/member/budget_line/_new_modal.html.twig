<div class="modal fade" id="budgetLineFormModal" tabindex="-1" aria-labelledby="budgetLineFormModalLabel" aria-hidden="true">
	<div class="modal-dialog">
		<div class="modal-content">
			<div class="modal-header">
				<h5 class="modal-title" id="budgetLineFormModalLabel">Nouvelle ligne budgétaire</h5>
				<button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Fermer"></button>
			</div>
			<turbo-frame id="budget-line-form-frame" loading="lazy" src="{{ path('app_budget_line_new', {'budgetSlug': budget.slug, 'organizationSlug': organization.slug}) }}">
				<div class="modal-body text-center py-5">
					<div class="spinner-border text-primary" role="status">
						<span class="visually-hidden">Chargement...</span>
					</div>
				</div>
			</turbo-frame>
		</div>
	</div>
</div>
