<form method="post" action="{{ path('app_soft_delete_budget_line', {'id': budgetLine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ budgetLine.id) }}">
    <button class="btn">
        {{ ux_icon('mdi:trash-can', {'class': 'me-2'}) }}Supprimer
    </button>
</form>
