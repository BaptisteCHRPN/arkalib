{# ── HERO SECTION ── #}
<section class="hero-budget pb-5 pt-4" aria-labelledby="hero-heading">
    <div class="container">
        <div class="row align-items-center">
            <div class="col-md-6 mb-4 mb-md-0">
                <h2 id="hero-heading" class="hero-budget-title">
                    Simplifiez vos<br aria-hidden="true">
                    budgets associatifs.
                </h2>
                <p class="hero-budget-subtitle">
                    Créez, suivez et gérez vos finances<br aria-hidden="true">
                    sans notions comptables.
                </p>
                <a href="{{ path('app_register') }}" class="btn btn-primary fs-4 pe-4 ps-4">
                    Essayer Arkalib
                </a>
            </div>
            <div class="col-md-6 d-flex justify-content-center">
                <img
                    src="{{ asset('img/hero.svg') }}"
                    alt="Illustration montrant la gestion simplifiée des finances associatives"
                    class="hero-budget-image img-fluid"
                    loading="lazy"
                >
            </div>
        </div>
    </div>
</section>