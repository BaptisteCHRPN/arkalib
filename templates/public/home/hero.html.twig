<section class="hero-budget py-5">
    <div class="container">
        <div class="row align-items-center">
            <!-- Colonne de texte -->
            <div class="col-md-6 mb-4 mb-md-0">
                <h2 class="hero-budget-title">
                    Simplifiez vos<br>
                    budgets associatifs.
                </h2>
                <p class="hero-budget-subtitle">
                    Créez, suivez et gérez vos finances<br>
                    sans notions comptables.
                </p>
                <a href="{{ path('app_register') }}" class="btn btn-primary fs-4 pe-4 ps-4">
                    Essayer Arkalib
                </a>
            </div>
            <!-- Colonne de l'illustration -->
            <div class="col-md-6 d-flex justify-content-center">
                <img
                    src="{{ asset('img/hero.svg') }}"
                    alt="Illustration de finances associatives"
                    class="hero-budget-image img-fluid"
                >
            </div>
        </div>
    </div>
</section>
